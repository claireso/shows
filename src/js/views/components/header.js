var React = require('react');

var Header = React.createClass({
  render: function () {
    return (
      <div class="row">
        <h1>Find a show</h1>
      </div>
    );
  }
});

module.exports = Header;
